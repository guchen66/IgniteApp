<Window x:Class="IgniteApp.Dialogs.Views.AlarmPopupView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IgniteApp.Dialogs.Views"
        mc:Ignorable="d"
        Title="AlarmPopupView" Height="450" Width="800">
    <StackPanel VerticalAlignment="Center">
        <TextBlock Text="{Binding CurrentAlarm}" FontSize="16" HorizontalAlignment="Center" />
        <TextBlock Text="{Binding TriggerTime,StringFormat='触发时间: {0:HH:mm:ss}'}"
                   Margin="0,10" />
        <Button Content="确认" Command="{Binding AcknowledgeCommand}"
                Width="80" Margin="0,20" />
    </StackPanel>
</Window>