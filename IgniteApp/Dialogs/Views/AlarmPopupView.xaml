<Window x:Class="IgniteApp.Dialogs.Views.AlarmPopupView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IgniteApp.Dialogs.Views"
        mc:Ignorable="d"
        Title="AlarmPopupView" Height="450" Width="800">
    <Window.Resources>
        <DataTemplate x:Key="Alarm">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding Id}" />
                <TextBlock Text="{Binding Name}" />
                <TextBlock Text="{Binding Solution}" />
                <TextBlock Text="{Binding TriggerTime,StringFormat='触发时间: {0:yyyy/MM/dd:HH:mm:ss}'}" />
                <TextBlock Text="{Binding AlarmLevel}" />
            </StackPanel>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <ListBox ItemsSource="{Binding AlarmList}" ItemTemplate="{StaticResource Alarm}">
        </ListBox>
        <StackPanel Grid.Row="1" VerticalAlignment="Center" Orientation="Horizontal">

            <Button Content="确认" Command="{Binding AcknowledgeCommand}"
            Width="80" Margin="0,20" />
        </StackPanel>
    </Grid>
</Window>