<UserControl
    x:Class="IgniteApp.Shell.ProcessParame.Views.LoadCalibrationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:local="clr-namespace:IgniteApp.Shell.ProcessParame.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:s="https://github.com/canton7/Stylet"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition />
            <RowDefinition Height="40" />
        </Grid.RowDefinitions>

        <WrapPanel>
            <Button
                Width="120"
                Height="30"
                Margin="5"
                Command="{s:Action StartCalibration}"
                Content="开始标定" />
            <Button
                Width="120"
                Height="30"
                Margin="5"
                Command="{s:Action PauseCalibration}"
                Content="暂停标定" />
            <Button
                Width="120"
                Height="30"
                Margin="5"
                Command="{s:Action ResumeCalibration}"
                Content="恢复标定" />
            <Button
                Width="120"
                Height="30"
                Margin="5"
                Command="{s:Action StopCalibration}"
                Content="停止标定" />
            <Label Content="上料" />
        </WrapPanel>

        <DataGrid
            Grid.Row="1"
            AutoGenerateColumns="False"
            ItemsSource="{Binding LoadCalibrationDataList}">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Id}" Header="Id" />
                <DataGridTextColumn Binding="{Binding CaliType}" Header="标定类型" />
                <DataGridTextColumn Binding="{Binding StartValue}" Header="X" />
                <DataGridTextColumn Binding="{Binding EndValue}" Header="Y" />
                <DataGridTextColumn Binding="{Binding CaliTime}" Header="标定时间" />
                <DataGridTextColumn Binding="{Binding Result}" Header="标定结果" />
            </DataGrid.Columns>
        </DataGrid>

        <WrapPanel Grid.Row="2">
            <Button Command="{s:Action SaveCalibrationData}" Content="保存" Width="100" Margin="5" />
            <Button Command="{s:Action Undo}" Width="100" Height="30" Content="撤销" Margin="5" />
            <Button Command="{s:Action SendDataToUnLoad}" Width="100" Height="30" Content="发送数据" Margin="5" />
        </WrapPanel>
    </Grid>
</UserControl>