<UserControl
    x:Class="IgniteApp.Shell.ProcessParame.Views.CO2TeachView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:convert="clr-namespace:IT.Tangdao.Framework.DaoConverters;assembly=IT.Tangdao.Framework"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:IgniteApp.Shell.ProcessParame.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:s="https://github.com/canton7/Stylet"
    xmlns:vm="clr-namespace:IgniteApp.Shell.ProcessParame.ViewModels"
    xmlns:control="clr-namespace:IgniteApp.Controls"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.Resources>
        <convert:BoolToColorConverter
            x:Key="BoolToColorConverter"
            FalseValue="Red"
            TrueValue="DodgerBlue" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel>
            <control:CustomStatusLight Background="{Binding IsStatusActive,Converter={StaticResource BoolToColorConverter}}" />
        </StackPanel>
        <StackPanel Grid.Row="1">
            <StackPanel>
                <TextBox Text="{Binding Name}" />

                <Button Margin="5" Command="{Binding SetCommand}" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>