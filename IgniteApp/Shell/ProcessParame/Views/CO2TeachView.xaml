<UserControl x:Class="IgniteApp.Shell.ProcessParame.Views.CO2TeachView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:IgniteApp.Shell.ProcessParame.Views"
             mc:Ignorable="d"
             xmlns:s="https://github.com/canton7/Stylet"
             xmlns:vm="clr-namespace:IgniteApp.Shell.ProcessParame.ViewModels"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <StackPanel>
            <StackPanel>
                <TextBox Text="{Binding Name}" />
                <Button Command="{s:Action ExecuteSet}" Margin="5" />
                <Button Command="{Binding SetCommand}" Margin="5" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>