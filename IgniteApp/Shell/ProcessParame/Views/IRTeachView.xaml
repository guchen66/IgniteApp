<UserControl x:Class="IgniteApp.Shell.ProcessParame.Views.IRTeachView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:IgniteApp.Shell.ProcessParame.Views"
             mc:Ignorable="d"
             xmlns:s="https://github.com/canton7/Stylet"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Menu Grid.Row="0">
            <MenuItem Header="Item1" Command="{Binding MenuCommand}">
                <MenuItem Header="1111" />
                <MenuItem Header="2222" />
                <MenuItem Header="3333" />
                <MenuItem Header="4444" />
            </MenuItem>
            <MenuItem Header="Item2" Command="{Binding MenuCommand}" />
            <MenuItem Header="Item3" Command="{Binding MenuCommand}" />
        </Menu>
        <DataGrid Grid.Row="1" ItemsSource="{Binding }"></DataGrid>

        <StackPanel Margin="20" Grid.Row="2">
            <TextBox Text="{Binding ResponseData}" />
            <Button Content="1. 生成快照" Command="{Binding ExecuteBinCommand}" />
            <Button Content="2. 加载快照" Command="{Binding OpenBinCommand}" />
        </StackPanel>
    </Grid>
</UserControl>