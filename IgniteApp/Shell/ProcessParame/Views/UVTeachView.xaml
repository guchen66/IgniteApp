<UserControl
    x:Class="IgniteApp.Shell.ProcessParame.Views.UVTeachView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="clr-namespace:IgniteApp.Behaviors"
    xmlns:convert="clr-namespace:IgniteApp.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:local="clr-namespace:IgniteApp.Shell.ProcessParame.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:s="https://github.com/canton7/Stylet"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.Resources>
        <convert:ForegroundBrushConverter x:Key="ForegroundBrushConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>
        <DataGrid
            x:Name="uvDataGrid"
            AutoGenerateColumns="False"
            ItemsSource="{Binding UvTeachModelList}"
            SelectionMode="Single"
            SelectionUnit="Cell">
            <i:Interaction.Behaviors>
                <behaviors:DataGridCurrentCellBehavior CurrentCell="{Binding CurrentCell, Mode=TwoWay}" OwnerGrid="{Binding OwnerGrid, Mode=OneWayToSource}" />
            </i:Interaction.Behaviors>
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Id}" Header="Id" />
                <DataGridTextColumn Binding="{Binding CutType}" Header="切割类型" />
                <DataGridTemplateColumn Header="Start">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding StartValue, UpdateSourceTrigger=PropertyChanged}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <TextBox Text="{Binding StartValue, UpdateSourceTrigger=PropertyChanged}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="End">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding EndValue, UpdateSourceTrigger=PropertyChanged}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <TextBox Text="{Binding EndValue, UpdateSourceTrigger=PropertyChanged}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Button
                Width="80"
                Height="30"
                Margin="5"
                Command="{Binding PrevPageCommand}"
                Content="上一页" />
            <TextBlock Text="{Binding PageInfo}"
                   VerticalAlignment="Center"
                   Margin="10,0"
                   FontWeight="Bold" />
            <Button
                Width="80"
                Height="30"
                Margin="5"
                Command="{Binding NextPageCommand}"
                Content="下一页" />
        </StackPanel>
    </Grid>
</UserControl>