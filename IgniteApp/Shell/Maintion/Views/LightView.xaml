<UserControl
    x:Class="IgniteApp.Shell.Maintion.Views.LightView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:be="clr-namespace:IgniteApp.Behaviors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:local="clr-namespace:IgniteApp.Shell.Maintion.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:s="https://github.com/canton7/Stylet"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <StackPanel>
            <hc:ProgressButton
                x:Name="pro"
                Width="180"
                Margin="5"
                be:ProgressButtonExtension.Value="{Binding ElementName=pro, Path=Progress}"
                Command="{s:Action ExecuteGetProcess}"
                Progress="{Binding Progress}"
                Content="0%"
                Style="{StaticResource ProgressButtonPrimary}" />
          
        </StackPanel>
    </Grid>
</UserControl>
