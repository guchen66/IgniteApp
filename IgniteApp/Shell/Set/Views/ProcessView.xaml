<UserControl
    x:Class="IgniteApp.Shell.Set.Views.ProcessView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:local="clr-namespace:IgniteApp.Shell.Set.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:s="https://github.com/canton7/Stylet"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/IgniteApp;component/Assets/DataTemplates/ToggleButtonTemplate.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>
        <hc:TabControl ItemsSource="{Binding ProcessItems}">
            <hc:TabControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}" />
                </DataTemplate>
            </hc:TabControl.ItemTemplate>
            <hc:TabControl.ContentTemplate>
                <!--  直接使用资源中的DataTemplate  -->
                <StaticResource ResourceKey="ProcessDetail" />
            </hc:TabControl.ContentTemplate>
        </hc:TabControl>
        <Grid Grid.Row="1">
            <Button Width="100" Height="30" Command="{s:Action RefreshProcessData}" />
            <Button Width="100" Height="30" VerticalAlignment="Bottom" HorizontalAlignment="Right" Content="保存" Command="{s:Action SaveProcessData}" />
        </Grid>
    </Grid>
</UserControl>