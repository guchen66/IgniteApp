<UserControl
    x:Class="IgniteApp.Shell.Footer.Views.FooterView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:be="clr-namespace:IgniteApp.Behaviors"
    xmlns:conver="clr-namespace:IT.Tangdao.Framework.Converters.Wpf;assembly=IT.Tangdao.Framework"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:local="clr-namespace:IgniteApp.Shell.Footer.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:s="https://github.com/canton7/Stylet"
    d:DesignHeight="450"
    d:DesignWidth="1200"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary />
            </ResourceDictionary.MergedDictionaries>
            <conver:BoolToColorConverter x:Key="deviceState" />
            <conver:StringToBrushConverter x:Key="bg" />
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <StatusBar Height="30">
            <StatusBarItem>
                <StackPanel Orientation="Horizontal">
                    <Ellipse
                        Width="10"
                        Height="10"
                        Margin="0,0,5,0"
                        Fill="{Binding IsConn, Converter={StaticResource deviceState}}" />
                    <TextBlock Text="PLC" />
                </StackPanel>
            </StatusBarItem>
            <Separator />
            <StatusBarItem>
                <StackPanel Orientation="Horizontal">
                    <Ellipse
                        Width="10"
                        Height="10"
                        Margin="0,0,5,0"
                        Fill="{Binding IsConn, Converter={StaticResource deviceState}}" />
                    <TextBlock Text="CCD" />
                </StackPanel>
            </StatusBarItem>
            <Separator />
            <StatusBarItem>
                <StackPanel Orientation="Horizontal">
                    <Ellipse
                        Width="10"
                        Height="10"
                        Margin="0,0,5,0"
                        Fill="{Binding IsConnServer, Converter={StaticResource deviceState}}" />
                    <TextBlock Text="服务端" />
                </StackPanel>
            </StatusBarItem>
        </StatusBar>

        <StackPanel Grid.Column="1" Orientation="Horizontal">
            <Button
                Margin="5"
                Background="{Binding BgColor, Converter={StaticResource bg}}"
                Command="{s:Action TestColor}"
                Content="测试颜色转换" />
            <Button
                Margin="5"
                Command="{s:Action TestMessage}"
                Content="测试弱引用组件" />
            <Button
                Margin="5"
                Command="{s:Action TestObserver}"
                Content="测试观察者模式组件" />
            <Button
                Margin="5"
                Command="{s:Action TestNoticeAll}"
                Content="测试一键通知" />
            <Button
                Margin="5"
                Command="{s:Action TestNotice}"
                Content="测试单个通知" />
            <Button
                Margin="5"
                Command="{s:Action TestWindow}"
                Content="测试窗体" />
            <Button
                Margin="5"
                Command="{s:Action Test2}"
                Content="测试2" />
            <Button
                Margin="5"
                Command="{s:Action OpenEQP}"
                Content="账料" />
            <Button
                Margin="5"
                Command="{s:Action OpenTTView}"
                Content="TT" />
            <Button
                Margin="5"
                Command="{s:Action OpenPhotoView}"
                Content="拍照" />
        </StackPanel>
    </Grid>
</UserControl>