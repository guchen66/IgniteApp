<UserControl
    x:Class="IgniteApp.Shell.Footer.Views.FooterView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:conver="clr-namespace:IT.Tangdao.Framework.DaoConverters;assembly=IT.Tangdao.Framework"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:local="clr-namespace:IgniteApp.Shell.Footer.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:be="clr-namespace:IgniteApp.Behaviors"
    xmlns:s="https://github.com/canton7/Stylet"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.Resources>
        <conver:BoolToColorConverter x:Key="deviceState" />
    </UserControl.Resources>
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <StatusBar Height="30">
            <StatusBarItem>
                <StackPanel Orientation="Horizontal">
                    <Ellipse
                      Width="10"
                      Height="10"
                      Margin="0,0,5,0"
                      Fill="{Binding IsConn, Converter={StaticResource deviceState}}" />
                    <TextBlock Text="PLC" />
                </StackPanel>
            </StatusBarItem>
            <Separator />
            <StatusBarItem>
                <StackPanel Orientation="Horizontal">
                    <Ellipse
                      Width="10"
                      Height="10"
                      Margin="0,0,5,0"
                      Fill="{Binding IsConn, Converter={StaticResource deviceState}}" />
                    <TextBlock Text="CCD" />
                </StackPanel>
            </StatusBarItem>
            <Separator />
            <StatusBarItem>
                <StackPanel Orientation="Horizontal">
                    <Ellipse
                      Width="10"
                      Height="10"
                      Margin="0,0,5,0">
                        <i:Interaction.Behaviors>
                            <be:ShapeBehavior IsConnected="{Binding ElementName=btn,Path=IsEnabled}" />
                        </i:Interaction.Behaviors>
                    </Ellipse>
                    <TextBlock Text="其他" />
                </StackPanel>
            </StatusBarItem>
        </StatusBar>

        <StackPanel Orientation="Horizontal" Grid.Column="1">
            <Button Command="{s:Action OpenEQP}" Content="账料" x:Name="btn" />
            <Button Command="{s:Action OpenTTView}" Content="TT" />
        </StackPanel>
    </Grid>
</UserControl>